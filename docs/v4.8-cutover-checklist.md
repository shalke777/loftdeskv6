# LoftDesk v4.8 cutover checklist

1. Run migrations 001-009 on staging.
2. Backfill `company_members` for existing owner profiles.
3. Verify `my_company_id()` and `my_role()` on staging users.
4. Run `scripts/rls-smoke.sql`.
5. Invite one test worker and one accountant.
6. Verify register/login/invite acceptance flows.
7. Verify Billing/Admin/KSeF feature gating against roles and plans.
8. Test portal token read + chat + accept/reject estimate.
9. Deploy v4.8 to staging subdomain.
10. Keep v3 read-only fallback during cutover window.

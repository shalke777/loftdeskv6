# LoftDesk v5.0 staging release plan

1. Uruchom `npm run preflight`
2. Otwórz `/release` i usuń blokady workspace
3. Przeleć `scripts/pre-cutover-snapshot.sql`
4. Zastosuj migracje 001-012
5. Uruchom `scripts/post-cutover-verify.sql` i `scripts/rls-smoke.sql`
6. Sprawdź zaproszenie `/join/:token`, portal i onboarding
7. Dopiero wtedy przełącz staging traffic

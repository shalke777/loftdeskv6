# v4.9 RLS / deploy pack

## Cel
Domknąć staging-ready przejście z legacy `user_id` do `company_id` bez ręcznego zgadywania kolejności działań.

## Kolejność
1. backup / snapshot
2. migracje 001-011
3. seed / invited members where needed
4. `scripts/rls-smoke.sql`
5. `scripts/post-cutover-verify.sql`
6. ręczny smoke z `tests/manual/v4.9-staging-checklist.md`

## Krytyczne warunki
- owner/admin muszą mieć dostęp do billing/settings/team
- worker nie może usuwać klientów/faktur/projektów
- portal token musi działać publicznie, ale bez dostępu do innych rekordów
- legacy profile bez `company_members` musi dalej działać przez bridge

# Supabase-first auth/settings notes

- `settings.api.ts` now prefers `company_invitations` and `company_members` when multi-tenant mode is available.
- `RegisterForm` shows pending invitations for the typed email to reduce accidental duplicate workspaces.
- `accept_company_invitation()` should be called from a secure RPC path in production.
- Keep service-role usage only inside protected server functions.
